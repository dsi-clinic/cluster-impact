# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Collector runtime state — raw records must NEVER reach this public repo
raw/
*.jsonl
*.jsonl.zst

# Secrets
*.pem
id_ed25519*
.env

.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Gas City
.beads/*
!.beads/identity.toml
# Symlink farm into this machine's ~/.gc cache — absolute paths, useless to
# anyone else and not something a public repo should carry.
.claude/skills/
